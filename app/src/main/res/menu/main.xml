<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_favorite"
        android:icon="@drawable/ic_directions_white_48px"
        android:title="akcija"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/showAllUsers"
        android:title="Show All Users"
        app:showAsAction="never"/>

    <item
        android:id="@+id/showFriends"
        android:title="Show Friends"
        app:showAsAction="never"/>

    <item
        android:id="@+id/showChallenge"
        android:title="Show Challenge"
        app:showAsAction="never"/>


</menu>